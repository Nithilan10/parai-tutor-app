Place **WAV or MP3** clips here, one folder per class:

- `big/` — Thi, Ku, Ka (big-stick strokes)
- `small/` — Tha (small stick)
- `both/` — Theem (both sticks)

Run `python model_training/bootstrap_parai_data.py` from the repo to fill starter files from `public/audio/`.

Full provenance notes: **`../DATA_SOURCES.md`**.
