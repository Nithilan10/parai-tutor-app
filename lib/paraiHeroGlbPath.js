/** Shared path for the Parai hero GLB (landing + backdrop). Override with NEXT_PUBLIC_HERO_GLB_URL. */
export const PARAI_HERO_GLB_PATH =
  process.env.NEXT_PUBLIC_HERO_GLB_URL || "/models/Parai.glb";
